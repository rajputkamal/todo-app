import styled from "styled-components";

const Avatar = () => {
  return <StyledAvatarContainer>KS</StyledAvatarContainer>;
};

const StyledAvatarContainer = styled.div`
  border-radius: 50%;
  border: 1px solid #c5c6c7;
  padding: 2px 5px;
`;

export { Avatar };
